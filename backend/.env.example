# Copy to .env — never commit real values.
SECRET_KEY=change-me
DEBUG=1
# Blank = local SQLite. Set to a PostgreSQL URL for production, e.g.
# DATABASE_URL=postgres://user:pass@host:5432/gatepass
DATABASE_URL=
TIME_ZONE=Africa/Harare
CORS_ALLOWED_ORIGINS=
